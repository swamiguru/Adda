# Presence counter. Without these the counter simply doesn't render,
# which is a fine state for local dev and preview deploys.
#
# Create a free Upstash Redis database, then copy the REST credentials.
# On Vercel: Storage tab -> Upstash Redis -> these get injected automatically.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
